server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = /__map_health {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    location / {
        try_files $uri =404;
    }
}
